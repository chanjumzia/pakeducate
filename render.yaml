services:
  - type: web
    name: pakeducate
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --workers 4 --bind 0.0.0.0:10000 app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0